# 组件规范 — `@affine/admin`（最小）

- 从 `@affine/admin/components/ui/*` 导入 UI（Button、Input、Label 等）。
- Toast：`sonner`（`toast.success` / `toast.error`）。
- 路由：`react-router-dom`（`Navigate`、admin 应用下的路由）。
- 表单：见 [form-guidelines.md](./form-guidelines.md)。
