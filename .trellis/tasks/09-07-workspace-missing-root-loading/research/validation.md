# 修复验证记录

分支：`codex/fix-workspace-missing-root-loading`

基线：`078521a61975e6dc985009850ed9dbe35dc864ec`

日期：2026-09-08

## 结果

| 检查                                             | 结果                              |
| ------------------------------------------------ | --------------------------------- |
| nbstore 全量、工作区状态、异常组件、移动菜单单测 | 19 文件 / 91 测试通过             |
| 浏览器桌面端回归                                 | 3/3 通过                          |
| Mobile Chrome 回归                               | 2/2 通过                          |
| core 及引用项目 TypeScript build                 | 通过，无诊断                      |
| 变更文件 ESLint                                  | 通过                              |
| 变更文件 oxlint --deny-warnings                  | 通过                              |
| i18n build                                       | 通过                              |
| Web production build                             | 通过，资源体积和入口体积 2 项警告 |

## 行为验证

- 空根读取完成后不再永远停留在加载界面。
- 现代库为空但 v1 有根时可正确迁移；无响应 Worker 会超时但不会被宣称数据丢失。
- 已可读的离线数据不会被后台同步错误遮挡。
- 根缺失时其他文档和附件保持原内容。
- 可以显式重载和切换健康工作区；移动菜单在外部关闭后仍可再次打开。
- 诊断字段白名单不包含原始错误或正文。

## 限制

- 本次没有恢复原先丢失的文档，也没有部署到线上。
- Electron/原生共享包完成类型检查，但没有执行原生设备运行测试。
- 本地 E2E 未启动后端，预期出现 localhost:3010 代理拒绝日志；合成本地工作区回归全部通过。

详细命令与测试场景见 ../implement.md。

## 单独复验入口

新增 `yarn test:workspace-loading`，只执行根数据缺失用例，禁止复用已有 8080 服务。本机复跑结果：`1 passed (35.9s)`。用例显式等待 reload 的 load 事件并保存截图。运行说明见 ../runnable-test.md。

## 提交前补充（2026-09-08）

- 独立 `yarn test:workspace-loading` 再次通过：1 passed（41.3 秒）。
- 全仓提交钩子发现 dev-main 原有的 5 处 oxlint 错误，位于 Copilot 导出文件及 TikZ 的 library.ts/runtime.ts；这 3 个文件此前与 HEAD 无差异。已做最小修正，并保留正常提交钩子。
- 补充修正后 TikZ 测试 12/12、core 及引用项目类型检查、Web 正式构建通过；构建仅有资源体积警告。
